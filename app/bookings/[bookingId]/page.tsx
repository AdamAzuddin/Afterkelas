const page = () => {
    //TODO(Dev 2 Team Teacher): Complete this function. First find the booking, check if number of reschedule exceeded limit (3), if yes don't allow, if no submit reschedule request to admin
    const rescheduleBooking = () => { 

    }

    //TODO: Make sure can mark attendance
  return (
    <div>page</div>
  )
}

export default page